// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema


generator client {
  provider = "prisma-client-js"
}
datasource db {
  provider = "postgresql"
}

model User {
  id        Int      @id @default(autoincrement())
  email     String   @unique
  username      String?
  password  String
  role      UserRole @default(USER)
  createdAt DateTime @default(now())
}

enum UserRole {
  USER
  ADMIN
}

model MusicStyle {
  id        Int        @id @default(autoincrement())
  name      String     @db.Citext // Case insensitive to enhance sorting
  playlists Playlist[]
  tracks    Track[]
}

model Playlist {
  id           Int      @id @default(autoincrement())
  name         String   @db.Citext
  creator      String
  clicks       Int      @default(0) //0 par defaut
  styleId      Int
  style        MusicStyle @relation(fields: [styleId], references: [id], onDelete: Cascade)

  contributors String[] //liste des noms de contributeurs en String
  tracks       PlaylistTrack[]
  createdAt    DateTime @default(now())
}

model Track {
  id         Int      @id @default(autoincrement())
  title      String
  artist     String
  album      String?
  genre      String?
  durationSeconds Int?
  coverUrl   String?
  audioUrl   String?
  styleId    Int
  style     MusicStyle @relation(fields: [styleId], references: [id], onDelete: Cascade)
  playlists PlaylistTrack[]
  addedAt    DateTime @default(now())// pour trier par heures
}

model PlaylistTrack {
  id         Int      @id @default(autoincrement())
  playlistId Int
  trackId    Int
  order      Int

  playlist   Playlist @relation(fields: [playlistId], references: [id], onDelete: Cascade)
  track      Track    @relation(fields: [trackId], references: [id], onDelete: Cascade)

  @@index([playlistId, order])
}