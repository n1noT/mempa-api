meta {
  name: healthcheck
  type: http
  seq: 2
}

head {
  url: {{url}}/healthcheck
  body: none
  auth: inherit
}

settings {
  encodeUrl: true
  timeout: 0
}

docs {
  Lightweight liveness check. Returns 200 with no body if the server is up.
}
