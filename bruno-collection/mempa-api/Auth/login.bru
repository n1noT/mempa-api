meta {
  name: login
  type: http
  seq: 2
}

post {
  url: {{url}}/auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "email": "nino.toutain@utoulouse.fr",
    "password": "nino.toutain@utoulouse.fr"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}

docs {
  Log in with email and password. On success, a session cookie (`sessionId`) is set and the user object is returned.

  Returns 401 if credentials are invalid.
}
