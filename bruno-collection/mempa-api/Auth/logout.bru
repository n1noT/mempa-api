meta {
  name: logout
  type: http
  seq: 4
}

post {
  url: {{url}}/auth/logout
  body: none
  auth: inherit
}

settings {
  encodeUrl: true
}

docs {
  Destroy the current session and clear the `sessionId` cookie.
}
