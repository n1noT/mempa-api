meta {
  name: me
  type: http
  seq: 3
}

get {
  url: {{url}}/auth/me
  body: none
  auth: inherit
}

settings {
  encodeUrl: true
  timeout: 0
}

docs {
  Return the currently authenticated user (`id`, `username`, `role`).

  Returns 401 if no active session exists.
}
