meta {
  name: create-style
  type: http
  seq: 3
}

post {
  url: {{url}}/styles
  body: json
  auth: inherit
}

body:json {
  {
    "name": "Jazz"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}

docs {
  Create a new music style. The `name` field is stored as case-insensitive (`Citext`).

  **Required fields:** `name`

  **Admin only.** Requires ADMIN role.
}
